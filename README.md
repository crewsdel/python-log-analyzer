# Python Log Analyzer

This is a simple personal project. It is a command-line Python tool that reads a log file and summarizes the number of INFO, WARNING, and ERROR events.

## Features

- Reads events from a text log
- Counts events by severity
- Handles missing files
- Prints a summary to the terminal

## Usage

```bash
python log_analyzer.py
